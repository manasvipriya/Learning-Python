def saydigit(n, arr):
    if n==0:
        return
    digit = n%10
    n = n//10

    saydigit(n, arr)

    print(arr[digit], end=" ")

arr = ['zero', 'one', 'two', 'three', 'four', 'five', 'six', 'seven', 'eight', 'nine']

n = int(input("Enter a number :- "))

saydigit(n, arr)
